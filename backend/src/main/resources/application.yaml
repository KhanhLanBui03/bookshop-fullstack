spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3314/bookshop_online?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: MY_SUPER_SECRET_KEY_123456789012345678901234567890
  access-expiration: 900000 # 15 phút
  refresh-expiration: 2592000000 # 30 ngày
server:
  port: 8686
